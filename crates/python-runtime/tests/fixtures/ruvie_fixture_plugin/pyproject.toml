[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ruvie-fixture-plugin"
version = "0.0.1"
description = "Pure-Python fixture for RuViE's future pip plugin lifecycle tests."
requires-python = ">=3.13,<3.14"
