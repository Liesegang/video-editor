name: Build and Package Rust App for macOS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@fcf085fcb4b4b8f63f96906cd713eb52181b5ea4

      - name: Install dependencies
        run: |
          brew install ffmpeg
          brew install vulkan-headers
          brew install vulkan-validationlayers

      # Homebrew's moving OpenColorIO formula is not an exact, redistributable
      # runtime contract. Keep app/opencolorio disabled until the pinned native
      # runtime and dylibs are bundled and tested in this installer.
      - name: Build Release (built-in color backend; no OpenColorIO runtime)
        run: cargo build -p app --release --no-default-features

      - name: Package
        run: |
          cargo install cargo-bundle || true
          cargo bundle --release
          mkdir -p installer
          cp target/release/bundle/*.dmg installer/

      - name: Upload Installer Artifact
        uses: actions/upload-artifact@v4
        with:
          name: installer-macos
          path: installer/
