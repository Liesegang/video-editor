[package]
name = "ruvie-python-runtime"
version = "0.1.0"
edition = "2024"
description = "Trusted in-process CPython runtime for RuViE automation and expressions."
license = "MIT"

[lints]
workspace = true

[dependencies]
lru = "0.16.3"
pyo3 = { version = "0.29.0", default-features = false }
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.23"
