uniform shader content;
uniform float2 redOffset;
uniform float2 blueOffset;

half4 main(float2 p) {
    float r = content.eval(p + redOffset).r;
    float g = content.eval(p).g;
    float b = content.eval(p + blueOffset).b;
    float a = content.eval(p).a;
    
    return half4(r, g, b, a);
}
